# 通知渠道：dingtalk（钉钉，推荐）、wxpusher（个人微信）、dingtalk,wxpusher（同时推送）
NOTIFY_CHANNEL=dingtalk

# 钉钉群机器人 Webhook，多个用英文逗号分隔
DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
# 若机器人安全设置为「加签」，填写 SECRET；选「自定义关键词」则留空
DINGTALK_SECRET=

# WxPusher 个人微信（NOTIFY_CHANNEL 含 wxpusher 时必填，免费）
WXPUSHER_APP_TOKEN=
WXPUSHER_TOPIC_IDS=
WXPUSHER_UIDS=

# 监测关键词，逗号分隔
KEYWORDS=锂电,新能源,碳酸锂

# 轮询间隔（秒），300 = 每 5 分钟
POLL_INTERVAL_SEC=300

# 每次拉取财联社电报条数（5 分钟间隔 20 条足够）
CLS_RN=20

# 金十期货 qihuo.jin10.com 监测（默认开启）
JIN10_ENABLED=true
# 可选：按分类筛选，留空表示综合快讯。示例：25=化工（含碳酸锂相关）
JIN10_CHANNEL=

# SQLite 状态库路径（可选）
STATE_DB_PATH=data/state.db
